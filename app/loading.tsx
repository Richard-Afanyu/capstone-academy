import React from "react";

const Loading = () => {
  return (
    <div className="h-[100vh] w-full flex items-center justify-center">
      <h2 className="smallTitle">Loading content...</h2>
    </div>
  );
};

export default Loading;
